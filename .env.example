# Copy to .env and fill in. Never commit .env.

# LLM (OpenAI by default; any compatible provider works)
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_MODEL_STRONG=gpt-4o

# Meta Threads API (obtained via Meta OAuth) — only needed to publish
THREADS_ACCESS_TOKEN=
THREADS_USER_ID=
